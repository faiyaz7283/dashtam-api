# Environment files in ROOT directory only (all variants except examples)
/.env
/.env.*
!/.env.*.example
/.env.local
/.env.*.local
/.env.backup

# Ignore .env directories (Python virtual envs) but NOT our env/ config directory
**/.env/

# Ignore ALL actual env files in env/ directory (they contain secrets)
# Only commit .example files
# This includes: .env.dev, .env.test, .env.ci, .env.prod
env/.env
env/.env.*
!env/.env.*.example

# Certificates
# Allow self-signed dev/test certs (.pem files are committed for CI/dev/test)
# Ignore production certs and private keys
certs/*.key
certs/*.crt
certs/prod*.pem
certs/production*.pem
certs/staging*.pem

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
# Virtual env directory (uppercase only, in subdirectories)
[E][N][V]/
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# UV
.uv/

# Docker
docker-compose.override.yml

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Database
*.db
*.sqlite
*.sqlite3

# Logs
*.log
logs/

# Testing
coverage.xml
*.cover
.hypothesis/

# Local development
.local/
tmp/site/

# MkDocs build output
site/

# GeoLite2 database (license prohibits redistribution)
data/geolite2/
